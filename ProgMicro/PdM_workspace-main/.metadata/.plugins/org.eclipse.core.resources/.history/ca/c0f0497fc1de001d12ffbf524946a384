#include "main.h"
#include "port.h"
#include "Console.h"

/**
  * @brief  The application entry point.
  * @retval int
  */
int main(void)
{
	/* MCU peripherals configurations */
	APISysInit();

	ConsoleInit();
	ConsoleClear();
	uint8_t pData[] = "Bienvenidos al proyecto de Deni \n";
	uint8_t pData2[] = "\r Amewooo ";
	uint8_t pData3[] = "\r Que sopa?";
	ConsolePrintStr(pData);
	ConsolePrintStr(pData2);
	ConsolePrintStr(pData3);
	HAL_Delay(1000);
	ConsoleClear();
	uint8_t op;
	ConsolePrintStr(pData);

	while (1)
	{
		ConsolePrintStr(pData2);
		ConsolePrintFloat(12.254);
		HAL_Delay(500);
		ConsolePrintStr(pData3);
		ConsolePrintFloat(0.25);
		HAL_Delay(500);
		if(ConsoleNewData()){
			op = ConsoleDataReceive();
			ConsolePrintStr(op);
		}
	}
}
